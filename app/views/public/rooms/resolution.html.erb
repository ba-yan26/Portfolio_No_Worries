<h1>解決一覧</h1>

<% @rooms.each do |room| %>
  <% if room.resolution_status == true %>
    <%= link_to room.title, room_path(room) %>
    <%= link_to "ルーム削除", room_path(room), method: :delete, data: { confirm: "本当に削除しますか？"} %>
  <% end %>
<% end %>